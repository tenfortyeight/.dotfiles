# Shared provider plugin cache.
# Without this every .terraform dir downloads its own copy of each provider
# (~46GB across 79 workspaces). With it they share one copy on disk.
plugin_cache_dir = "$HOME/.terraform.d/plugin-cache"
